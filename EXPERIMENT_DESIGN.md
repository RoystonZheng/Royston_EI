# 实验设计说明

这个项目的实验目标不是证明 AI 一定比传统算法强，而是证明传统搜索/采样算法在更接近真实小车的条件下会暴露工程短板。

## 共同变量

每个页面都可以切换两类变量：

```text
实验强度：普通 / 困难 / 极限
环境模式：训练场景 / 扰动场景 / 泛化场景
```

三种环境模式含义：

```text
训练场景：地图和训练样本接近，观察算法本身特点。
扰动场景：障碍位置、速度、小车姿态发生变化，观察鲁棒性。
泛化场景：地图布局变得更陌生，障碍更密、更快，小车更大，观察泛化性。
```

页面左侧是传统算法，右侧是 AI 训练策略。左侧会保留搜索过程、采样树、旧路径、碰撞点等痕迹；右侧展示训练后的动作轨迹和 `.pt` 检查点状态。所有图都按时间推进，不只展示最终结果。

本轮升级还加入了三个共同约束：

```text
1. 每个实验都有移动障碍物。
2. 每次点击“运行”都会刷新一张新地图。
3. 传统算法和 AI 独立结束，一边完成后另一边继续跑。
```

机器人也不再是没有面积的点。网格实验会显示机器人半径，RRT 会显示安全半径，小车实验会显示长宽、安全边界和更严格的最小转弯半径。

页面里的 AI 训练分成两层：

```text
Python 训练：生成真实的 .pt 检查点文件。
网页演示训练：每次点击“运行”增加一轮训练，侧栏会更新训练轮次、loss 和成功率估计。
```

“重置训练”按钮会清空当前实验、当前难度、当前环境模式下的网页演示训练成果。

## Dijkstra 实验

左侧：Dijkstra 从起点向外扩散，不知道终点方向。

右侧：AI 策略学习专家路径，运行时直接给动作轨迹。

重点观察：

```text
搜索格子数
路径长度
机器人半径
移动障碍数
地图编号
AI 训练轮次
环境扰动后搜索范围是否扩大
AI 临场搜索是否明显更少
```

证明的缺陷：Dijkstra 稳定、可解释，但没有方向感。地图变大、障碍变多时，它会把很多和目标无关的区域也搜索一遍；当真实环境里还有移动障碍时，它本身并不会预测障碍未来位置。

## A* 动态障碍实验

左侧：A* 在当前地图上规划路径，障碍移动后旧路径失效，需要重新规划。

右侧：AI 动态策略在训练样本里学过等待、绕行、避开快障碍。

重点观察：

```text
重新规划次数
路径失效次数
等待/无路次数
机器人半径
移动障碍数
地图编号
AI 训练轮次
最快障碍速度
模拟延迟
```

证明的缺陷：A* 在静态地图里很强，但面对高速动态障碍时，它必须频繁重算。真实小车还有自身半径、感知延迟和计算延迟，路径刚算完就可能过期。

## RRT 窄通道实验

左侧：RRT 随机采样并长出搜索树，通过窄门需要采到关键区域。

右侧：AI 引导采样把更多采样点放到窄门附近。

重点观察：

```text
窄门宽度
机器人安全半径
移动障碍数
地图编号
AI 训练轮次
迭代次数
树节点数
20 次快速试验成功率
```

证明的缺陷：RRT 能处理连续空间，但结果不稳定。同一张地图多跑几次，可能有时很快成功，有时长时间失败；当机器人有安全半径时，窄门更难过。

## 网格路径 vs 小车实验

左侧：A* 找到格子路径，小车按这条直角路径跟踪。

右侧：AI 小车控制输出连续转向轨迹。

重点观察：

```text
车体尺寸
安全边界
最小转弯半径
移动障碍数
地图编号
AI 训练轮次
走廊宽度
直角转弯次数
最大跟踪偏离
碰撞点
```

证明的缺陷：传统网格路径通常把小车当成点，只证明“中心点能走”。真实小车有长宽、安全边界和转弯半径，中心点能过不代表车身能过；转弯半径越严格，直角路径越难执行。

## 鲁棒性、泛化性、可解释性怎么讲

鲁棒性：环境有小变化时还能不能稳定完成任务。比如障碍稍微移动、速度变快、小车初始角度变化后，算法是否还能安全到达。

泛化性：遇到训练外的新地图、新通道、新障碍组合时还能不能工作。传统算法通常能重新搜索，但计算成本可能上升；AI 推理快，但必须检验训练外表现。

可解释性：能不能说明为什么这样走、为什么失败。传统算法的搜索留痕很直观；AI 需要额外展示轨迹、动作、失败案例或注意区域，才能让人相信它不是黑箱乱走。
